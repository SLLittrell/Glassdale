import { getCriminals, useCriminals } from './criminals/CriminalProvider.js'

export const CriminalList = () => {
    getCriminals().then(passedCriminals => {
        return  
       
        
        
        /*
            Now that you have the data, what
            component should be rendered?
        */
    )
}